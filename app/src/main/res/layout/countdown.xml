<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin">

    <com.blackcrowsteam.musicstop.views.CircleView
        android:id="@+id/countdown_circle"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <com.blackcrowsteam.musicstop.views.ClockTextView
        android:id="@+id/countdown_text"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_alignParentStart="true"
        android:layout_gravity="center"
        android:gravity="center"
        android:shadowColor="@color/bpRed"
        android:text="00:00"
        android:textSize="@dimen/countdown_text_size"
        app:useClockTypeface="true" />

    <android.support.design.widget.FloatingActionButton
        android:id="@+id/countdown_fab"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBottom="@+id/countdown_circle"
        android:layout_alignEnd="@+id/countdown_circle"
        android:clickable="true"
        android:src="@drawable/play"
        app:backgroundTint="@color/hightlight"
        app:layout_anchorGravity="bottom|right|end"
        app:rippleColor="@color/hightlight" />

</RelativeLayout>
