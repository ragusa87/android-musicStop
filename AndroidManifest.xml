<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.blackcrowsteam.musicstop2"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk android:minSdkVersion="7" android:targetSdkVersion="15" />

    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name" >

        <activity
            android:name="com.blackcrowsteam.musicstop2.StopActivity"
            android:theme="@style/SampleTheme2" 
            android:label="@string/app_name" 
            
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            
            <intent-filter>
                <action android:name="com.blackcrowsteam.musicstop2.STOP" />
            </intent-filter>
        </activity>
        <activity
            android:theme="@style/SampleTheme2" 
            android:name="com.blackcrowsteam.musicstop2.Preferences"
            android:label="Prefs" >
            <intent-filer>
                <action android:name=".Preferences" />
                <category android:name="android.intent.cetagory.PREFERENCE" />
            </intent-filer>
        </activity>

        <service android:name="com.blackcrowsteam.musicstop2.StopService" >
        </service>
        
    </application>

</manifest>